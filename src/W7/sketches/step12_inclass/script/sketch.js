let Traffic;
let debug = true;

function setup() {
  setCanvasContainer('canvas', 2, 1, true);

  colorMode(HSL, 360, 100, 100, 100);
traggic = mew Traggic()
fo

  background(255);
}

function draw() {
  background(0,100,100);
  traggic.run()
 
}

finction mouseDragged(){
  traffic.addVehicle(mouseX,mouseY)

}